% Feasibility report — replacing the external LLM reasoner.
%
% Build (tectonic fetches packages on demand; no local TeX install needed):
%   tectonic -X compile docs/30-narrator-feasibility-report.tex \
%       --outdir outputs/feasibility
%
% Figures are transcribed from:
%   outputs/reasoner_comparison_real/{narrator_arch_sweep,faithfulness_audit}.json
%   outputs/generalizability/generalizability_report.json
%   docs/28-reasoner-cnn-vs-distilledqwen.md, docs/29-narrator-verification-and-gaps.md

\documentclass[11pt,a4paper]{article}

\usepackage[a4paper,margin=24mm,top=25mm,bottom=23mm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{XCharter}
\usepackage{inconsolata}
\usepackage[table]{xcolor}
\usepackage{booktabs,tabularx,array,longtable}
\usepackage{float}
\usepackage{titlesec,enumitem,microtype}
\usepackage{tcolorbox}
\tcbuselibrary{breakable}
\usepackage{fancyhdr}
\usepackage{caption}
\usepackage[colorlinks=true,allcolors=accent]{hyperref}

% ── palette: cool clinical neutrals, teal accent, semantic verdict colours ──
\definecolor{accent}{HTML}{0E7C86}
\definecolor{goGreen}{HTML}{1B7F4B}
\definecolor{holdAmber}{HTML}{8A5A0F}
\definecolor{stopRed}{HTML}{AC3128}
\definecolor{inkMuted}{HTML}{4A5860}
\definecolor{ruleGrey}{HTML}{D3DCE1}
\definecolor{softGrey}{HTML}{F2F6F7}
\definecolor{softTeal}{HTML}{E4F1F2}

\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\verd}[2]{\textbf{\textcolor{#1}{#2}}}
\newcommand{\eyebrow}[1]{%
  {\footnotesize\sffamily\bfseries\textcolor{accent}{\MakeUppercase{#1}}}}

\captionsetup[table]{
  font={small,color=inkMuted}, labelfont={bf,color=inkMuted},
  justification=raggedright, singlelinecheck=false, skip=5pt}

\titleformat{\section}{\Large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{0.6em}{}
\titlespacing*{\section}{0pt}{20pt}{7pt}
\titlespacing*{\subsection}{0pt}{13pt}{4pt}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\footnotesize\sffamily\textcolor{inkMuted}{Feasibility: replacing the external LLM reasoner}}
\fancyhead[R]{\footnotesize\sffamily\textcolor{inkMuted}{\thepage}}

\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}
\renewcommand{\arraystretch}{1.25}
% a little slack so hyphen-dense lines stretch instead of spilling past the margin
\setlength{\emergencystretch}{2em}

\newtcolorbox{callout}[1][accent]{
  colback=softGrey, colframe=#1, boxrule=0pt, leftrule=2.5pt,
  arc=0pt, outer arc=0pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  breakable}

\begin{document}

% ── masthead ──
\begin{flushleft}
\eyebrow{Feasibility assessment \quad\textbullet\quad RetinalAI screening pipeline}

\vspace{6pt}
{\huge\bfseries Replacing the external LLM reasoner\par}

\vspace{8pt}
{\large\color{inkMuted} Can the pipeline's two LLM-delegated jobs --- structured
triage and a free-text clinical narrative --- be served by self-contained
components? Both now ship. Triage is settled and enabled; the narrative ships
behind an opt-in flag because the instrumentation still cannot tell a good
narrator from a bad one.\par}

\vspace{10pt}
{\footnotesize\sffamily\color{inkMuted}
Rev.~2 \textbullet\ 12 August 2026 \quad\textbar\quad
Teacher: self-hosted Qwen3-8B-AWQ (vLLM) \quad\textbar\quad
Data: 240 RFMiD images \quad\textbar\quad
Status: triage shipped, narrator opt-in\par}
\end{flushleft}

\vspace{2pt}
{\color{ruleGrey}\hrule height 1.5pt}
\vspace{10pt}

\begin{callout}
\eyebrow{What changed since Rev.~1}

\vspace{3pt}
Rev.~1 concluded that triage was ready and the narrative was not qualifiable. The
triage half is now \textbf{shipped} and baked into all three images. On the narrative
half, two of the five blockers are closed and the edge verdict has moved from
``not feasible'' to ``reachable at a cost'': a prose-only, vocabulary-pruned narrator
reaches \textbf{54.2\,MB} with a generation rate of 1.000, where the previous
4-bit build scored 0.000. It is shipped \emph{off by default} --- 4-bit costs
42\% omission, and even bf16 drifts off-distribution. \S7 revises the
recommendation; \S6 is new.
\end{callout}

% ── verdict board ──
\begin{center}
\begin{tabularx}{\textwidth}{@{}L{51mm}L{31mm}Y@{}}
\toprule
\textbf{Component} & \textbf{Verdict} & \textbf{Basis} \\
\midrule
Triage --- structured decision &
\verd{goGreen}{Shipped} &
2\,KB model, enabled by default, baked into all three images. Reproduces the 8B
teacher exactly; validated out-of-sample on 160 unseen images. \\
\addlinespace[2pt]
Narrative --- grounded template &
\verd{goGreen}{Shipped (default)} &
0\,MB, deterministic, grounded by construction. Remains the default narrative
path. \\
\addlinespace[2pt]
Narrative --- server-hosted LLM &
\verd{holdAmber}{Shipped, opt-in} &
213.5\,MB bf16, published to the Hub and wired behind
\code{NARRATOR\_ENABLED=false}. Three of five blockers remain. \\
\addlinespace[2pt]
Narrative --- on-device / edge &
\verd{holdAmber}{Reachable, not advisable} &
54.2\,MB clears the 60\,MB gate, but drops reported findings in 42\% of cases.
GGUF is the untested route that may avoid that cost (\S6). \\
\bottomrule
\end{tabularx}
\end{center}

\section{What was actually assessed}

The pipeline delegates two jobs to an external LLM. They are not the same problem
and cannot share a verdict: \textbf{triage} is a four-way classification over the
disease classifier's structured output; \textbf{narrative} is generation that no
classifier can produce.

Everything below rests on measurements taken against a live, self-hosted
Qwen3-8B-AWQ teacher on RTX A6000 hardware, not on estimates. Where a figure is
arithmetic rather than a measurement, it is labelled.

\begin{table}[H]
\caption{Evidence base. Sample size remains the binding limitation on the
narrative half.}
\begin{tabularx}{\textwidth}{@{}Yr L{58mm} l@{}}
\toprule
\textbf{Assessment} & \textbf{n} & \textbf{Design} & \textbf{Strength} \\
\midrule
Triage --- out-of-sample       & 160 & Fresh unseen images, different mix    & \textbf{Strong} \\
Triage --- cross-validation    & 80  & 5-fold $\times$ 5 seeds (25 fits)     & Strong \\
Triage --- emergency stress    & 80  & Injected CRAO/AION                    & Synthetic, decisive \\
Narrative --- variant sweep    & 24  & 6 variants, one code path             & \textcolor{stopRed}{Weak} \\
Narrative --- faithfulness     & 24  & Claim-level audit                     & \textcolor{stopRed}{Weak} \\
Narrative --- off-distribution & 1   & Hand-built single-finding probe       & \textcolor{stopRed}{Anecdote} \\
\bottomrule
\end{tabularx}
\end{table}

\section{Triage --- shipped}

A 2\,KB structured-feature logistic head reproduces the teacher's priority
exactly: accuracy and macro-precision \textbf{1.000} held-out, \textbf{1.000
$\pm$ 0.000} under repeated cross-validation, and \textbf{1.000} out-of-sample on
160 previously unseen images with a different label mix. The image CNN it
replaces scored 0.792 / 0.492 at 6.1\,MB and 161\,ms.

\begin{callout}
\eyebrow{Why it is this cheap}

\vspace{3pt}
On the out-of-sample set the teacher's triage equals the classifier's own
\code{referral\_priority} in \textbf{160 of 160 cases}. The 8B model adds nothing
to the triage \emph{decision}, so a 2\,KB linear model, the deterministic rules
and an 8B LLM all score identically. This is a cost elimination, not a modelling
triumph.
\end{callout}

\textbf{Shipped as:} JSON weights rather than a pickle --- the model is one matrix
multiply, so serving needs neither scikit-learn nor a pickle load at startup. The
exporter refuses to write weights that disagree with the fitted estimator on all
240 available cases, and the loader refuses an artifact whose feature layout has
drifted. An emergency code escalates through a deterministic override, since
EMERGENCY never appeared in training and the head cannot emit it.

\section{Narrative --- what shipped, and what it costs}

Two changes made the narrative half tractable.

\textbf{Prose-only output.} Every reliability defect measured was a
malformed-JSON defect, and the JSON existed only to carry the triage fields ---
which the 2\,KB head now serves. A narrator emitting plain prose has no
structured contract left to break. That is a stronger guarantee than
grammar-constrained decoding (which prevents \emph{invalid} JSON) and needs no
extra dependency.

\textbf{Vocabulary pruning.} bitsandbytes leaves the embedding table unquantized,
which was the entire size gap: 28.3M embedding params at bf16 (56.6\,MB) on top
of a 53.1\,MB 4-bit body predicts 110\,MB, and 109.8\,MB was measured. Pruning
the vocabulary to the tokens this task can reach --- \textbf{49{,}152 $\to$ 920}
--- collapses the embedding to under 1\,MB. It is done by id remapping rather
than tokenizer surgery, so it is exact and reversible.

\begin{table}[H]
\caption{Compact narrator, 24 held-out cases. Generation rate is 1.000 at every
precision --- the axis that previously discriminated is now flat, so fidelity
decides.}
\begin{tabularx}{\textwidth}{@{}Yrrrrr@{}}
\toprule
\textbf{Precision} & \textbf{Size} & \textbf{Gen.\ rate} & \textbf{Omission} &
\textbf{Bad probs} & \textbf{Acuity drift} \\
\midrule
\rowcolor{softTeal}
bf16 (shipped) & 213.5\,MB & 1.000 & \textbf{0.000} & \textbf{0.000} & 0.250 \\
int8           & 107.3\,MB & \multicolumn{4}{l}{\textcolor{stopRed}{37.5\,s/case --- evaluation impractical}} \\
nf4            & \textbf{54.2\,MB} & 1.000 & \textcolor{stopRed}{\textbf{0.417}} &
\textcolor{stopRed}{0.167} & \textcolor{stopRed}{0.583} \\
\bottomrule
\end{tabularx}
\end{table}

\begin{callout}[stopRed]
\eyebrow{Two reasons the narrator ships disabled}

\vspace{3pt}
\textbf{4-bit trades a visible failure for an invisible one.} It clears the
60\,MB gate and always produces fluent text --- while dropping findings the
teacher reported in 42\% of cases. Fluent omission is harder to catch than a
blank output.

\vspace{4pt}
\textbf{bf16's clean scores do not generalise.} The audit set is dominated by
multi-finding cases resembling training data. Given a \emph{single}-finding case
by hand, bf16 produced ``a 75\% detection of diabetic retinopathy with a
prevalence of 88\%'' --- for one finding at 88\%. Both figures invented. One
probe is not a measurement, but it shows the 0.000 belongs to that split rather
than to the model.
\end{callout}

\section{Blockers --- two closed, three open}

\begin{table}[H]
\begin{tabularx}{\textwidth}{@{}L{52mm}L{22mm}Y@{}}
\toprule
\textbf{Blocker} & \textbf{Status} & \textbf{Detail} \\
\midrule
Structured output best-effort &
\verd{goGreen}{Closed} &
Prose-only output removes the parse-failure mode outright. Generation rate 1.000
at every precision. \\
Compliance statement dropped &
\verd{goGreen}{Closed} &
0 of 80 teacher traces carry the AI-disclosure sentence, so it is appended
deterministically in \code{src/narrator/service.py} rather than trusted to the
model. \\
No serving stack &
\verd{stopRed}{Open} &
Still \code{transformers.generate()} at batch 1. No defensible latency,
throughput or cost model. \\
Evaluation below clinical standard &
\verd{stopRed}{Open} &
One lexical check plus a first-pass claim audit. No NLI/judge cascade, no
clinician review. \\
Statistical power &
\verd{stopRed}{Open} &
24 held-out cases, 56 training rows, against $\sim$3{,}200 available images and a
free teacher. \\
\bottomrule
\end{tabularx}
\end{table}

\section{Deployment infrastructure}

The Hugging Face Space had been failing since 23 June for two reasons, both in
the deploy workflow and both now fixed and verified by a green CI run:

\begin{enumerate}[leftmargin=*,itemsep=3pt]
\item \code{short\_description} was 78 characters against a 60-character Hub
limit and contained an unquoted colon, so the README failed YAML validation on
every deploy and the Space never reached a build.
\item \code{rsync} ran without \code{--delete}, so a stale
\code{frontend/src/app/page.tsx} referencing a removed store field survived and
broke \code{next build} once builds actually started.
\end{enumerate}

The Space now serves: \code{/health} reports \code{model\_loaded: true} across 45
diseases. Both Docker images (CPU and GPU) build and push with the triage head
baked in. Crane Cloud deployment is out of scope for this revision.

\section{Format evaluation --- AWQ, GPTQ, GGUF}

Rev.~1 recommended AWQ/GPTQ over bitsandbytes. Evaluating that properly reverses
the recommendation, for a reason specific to this model: a 135M SmolLM2 is
\textbf{21\% embedding}, and what each format does with that table decides the
footprint.

\begin{table}[H]
\caption{Projections are arithmetic from published quantization layouts, not
measurements --- none of the three could be run on this host.}
\begin{tabularx}{\textwidth}{@{}YL{30mm}L{38mm}r@{}}
\toprule
\textbf{Format} & \textbf{Body} & \textbf{Embedding} & \textbf{Total} \\
\midrule
bnb NF4 & 4-bit & fp16, not quantized & 110\,MB \\
bnb int8 & int8 & fp16, not quantized & 107\,MB \\
AWQ / GPTQ (W4A16) & 4-bit & fp16, conventionally excluded & $\sim$110\,MB \\
\rowcolor{softTeal}
\textbf{GGUF \code{Q4\_K\_M}} & $\sim$4.5-bit & \textbf{quantized too} & $\sim$\textbf{80\,MB} \\
\bottomrule
\end{tabularx}
\end{table}

\textbf{AWQ and GPTQ would not beat bitsandbytes on size here.} They exclude the
embedding exactly as bnb does, landing at the same $\sim$110\,MB; their advantage
is throughput via fused kernels, which does not address the binding constraint.
\textbf{GGUF is the only one of the three that quantizes the embedding}, making
\code{Q4\_K\_M}/\code{Q5\_K\_M} the most promising route into the 60--100\,MB band
\emph{without} vocabulary surgery --- and therefore without the 42\% omission
cost that pruning-plus-4-bit incurred.

\textbf{Pruning and standard formats are alternatives, not complements.} The
pruned checkpoint deliberately breaks the model/tokenizer correspondence
(\code{vocab\_size} 920 against a 49{,}152-token tokenizer, bridged by an explicit
id remap). GGUF bakes the tokenizer into the file, so conversion would emit
something whose ids do not mean what the weights expect; AWQ/GPTQ would run
mechanically but still need the custom wrapper, losing the vLLM/SGLang serving
compatibility that justifies adopting them.

\textbf{Tooling status (measured).} \code{autoawq} 0.2.9 installs but cannot
import against transformers 4.57.1 and is unmaintained upstream;
\code{gptqmodel} and \code{llmcompressor} require torch $\geq$ 2.8;
\code{compressed-tensors} 0.11.0 is present as a vLLM dependency; llama.cpp
binaries (\code{llama-quantize}, \code{convert\_hf\_to\_gguf.py}) are absent, and
the \code{gguf} Python library alone writes only F16, not K-quants.

\section{Recommendation}

\textbf{Triage: done.} It is validated, effectively free, and removes an LLM
dependency from the decision path outright.

\textbf{Narrative: keep the template as the default and leave
\code{NARRATOR\_ENABLED=false}.} The bf16 narrator is shipped and usable for
evaluation, but nothing has changed about the reason not to enable it: the
instrumentation still cannot distinguish a good narrator from a bad one, and in a
clinical screening context that is the thing that has to work first. The
off-distribution probe (\S3) is a concrete reminder that clean held-out numbers
are not evidence of general reliability.

\textbf{Next, in order:} (1) regenerate traces at scale with the disclosure
mandated --- it closes the statistical-power and compliance gaps together and the
teacher is free; (2) build a real faithfulness evaluation with clinician review;
(3) try GGUF \code{Q4\_K\_M} for the 60--100\,MB target rather than more
bitsandbytes; (4) measure under vLLM before quoting any latency. Only then
revisit enabling the narrator by default.

The honest summary is unchanged from Rev.~1: the expensive component turned out
to be unnecessary for triage, and not yet trustworthy for narrative. The first is
now banked.

\vspace{14pt}
% keep the colophon on the final text page rather than orphaning it
\enlargethispage{3\baselineskip}
\vspace{9pt}
{\color{ruleGrey}\hrule height 0.6pt}
\vspace{4pt}

{\footnotesize\sffamily\color{inkMuted}\setlength{\parskip}{3pt}
\textbf{Sources.}\\
\code{docs/28-reasoner-cnn-vs-distilledqwen.md}\\
\code{docs/29-narrator-verification-and-gaps.md}\\
\code{outputs/reasoner\_comparison\_real/} \quad
\code{outputs/generalizability/}

\textbf{Provenance.} Measurements on RTX A6000 against a live Qwen3-8B-AWQ
teacher. Quantization-format sizes in \S6 are arithmetic, not measurements, and
are labelled as such. Effort estimates carry no measurement behind them.\par}

\end{document}
